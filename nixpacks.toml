# Nixpacks configuration for Railway
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm ci && cd crypto-billionaire-bot && npm ci --legacy-peer-deps"]

[start]
cmd = "cd crypto-billionaire-bot && node server.js"
